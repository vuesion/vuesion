# interfaces/api

**This directory is not required, you can delete it if you don't want to use it.**

The directory should only contain simple interfaces to communicate between the backend and frontend.
