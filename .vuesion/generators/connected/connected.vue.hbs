<template>
  <div :class="$style.{{ camelCase componentName }}">
    <vue-grid>
      <vue-breadcrumb :items="breadCrumbItems" />

      <vue-grid-row>
        <vue-grid-item>
          <vue-headline level="1">{{ properCase componentName }}</vue-headline>
        </vue-grid-item>
      </vue-grid-row>
    </vue-grid>
  </div>
</template>

<script lang="ts">
import VueGrid from '@/components/VueGrid/VueGrid.vue';
import VueBreadcrumb from '@/components/shared/VueBreadcrumb/VueBreadcrumb.vue';
import VueGridRow from '@/components/VueGrid/VueGridRow/VueGridRow.vue';
import VueGridItem from '@/components/VueGrid/VueGridColumn/VueGridColumn.vue';
import VueHeadline from '@/components/VueHeadline/VueHeadline.vue';

export default {
  metaInfo:   {
    title: '{{ properCase componentName }}',
  },
  components: {
    VueGrid,
    VueBreadcrumb,
    VueGridRow,
    VueGridItem,
    VueHeadline,
  },
  data: (): any => ({}),
  methods: {},
  computed: {
    breadCrumbItems() {
      return [
        { label: this.$t('common.home' /* Home */), href: '/' },
        { label: this.$t('common.{{ properCase singularName }}' /* {{ properCase singularName }} */), href: '/{{ dashCase singularName }}' },
      ];
    },
  },
};
</script>

<style lang="scss" module>
@import "~@/app/shared/design-system";

.{{ camelCase componentName }} {
  margin-top: $nav-bar-height;
  min-height: 500px;
}
</style>
