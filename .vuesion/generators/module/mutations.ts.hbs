import { I{{ properCase singularName }}State } from './state';

export interface I{{ properCase singularName }}Mutations {
  /**
   * put your mutations names, parameters and return values here, for example:
   * MY_MUTATION(context: ActionContext<I{{ properCase singularName }}State, IState>, param: any): void;
   */
}

export const {{ properCase singularName }}Mutations: I{{ properCase singularName }}Mutations = {
  /**
   * here comes the implementation of your mutations, for example:
   * MY_MUTATION: (state, param) {
   *   return state.myAttribute = param;
   * }
   */
};
