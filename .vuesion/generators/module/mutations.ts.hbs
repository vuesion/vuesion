import { I{{ properCase moduleName }}State } from './state';

export interface I{{ properCase moduleName }}Mutations {
  /**
   * put your mutations names, parameters and return values here, for example:
   * MY_MUTATION(context: ActionContext<I{{ properCase moduleName }}State, IState>, param: any): void;
   */
}

export const {{ properCase moduleName }}Mutations: I{{ properCase moduleName }}Mutations = {
  /**
   * here comes the implementation of your mutations, for example:
   * MY_MUTATION: (state, param) {
   *   return state.myAttribute = param;
   * }
   */
};
