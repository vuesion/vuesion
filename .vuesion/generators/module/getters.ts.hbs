import { I{{ properCase moduleName }}State } from './state';

export interface I{{ properCase moduleName }}Getters {
  /**
   * put your getter names, parameters and return values here, for example:
   * myAction(context: ActionContext<I{{ properCase moduleName }}State, IState>, param: any): Promise<any>;
   */
}

export const {{ properCase moduleName }}Getters: I{{ properCase moduleName }}Getters = {
  /**
   * here comes the implementation of your getters, for example:
   * myGetter(state: I{{ properCase moduleName }}State) {
   *   return state.myAttribute;
   * }
   */
};
