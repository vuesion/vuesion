import { I{{ properCase moduleName }}State } from './state';

export interface I{{ properCase moduleName }}Mutations {
  SET_INCREMENT_PENDING(state: I{{ properCase moduleName }}State, pending: boolean): void;

  SET_DECREMENT_PENDING(state: I{{ properCase moduleName }}State, pending: boolean): void;

  SET_COUNT(state: I{{ properCase moduleName }}State, count: number): void;
}

export const {{ properCase moduleName }}Mutations: I{{ properCase moduleName }}Mutations = {
  SET_INCREMENT_PENDING: (state: I{{ properCase moduleName }}State, pending: boolean) => {
    state.incrementPending = pending;
  },
  SET_DECREMENT_PENDING: (state: I{{ properCase moduleName }}State, pending: boolean) => {
    state.decrementPending = pending;
  },
  SET_COUNT:             (state: I{{ properCase moduleName }}State, count: number) => {
    state.count = count;
  },
};
