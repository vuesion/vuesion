import axios from 'axios';
import { ActionContext } from 'vuex';
import { IState } from '../mutations';

export interface I{{ properCase moduleName }}Actions {
  get{{ properCase moduleName }}(context: ActionContext<IState, IState>): void;
}

export const {{ properCase moduleName }}Actions: I{{ properCase moduleName }}Actions = {
  get{{ properCase moduleName }}({ commit, state }: ActionContext<IState, IState>) {
    return axios.get('https://jsonplaceholder.typicode.com/posts')
      .then((response: any) => {
        commit('{{ upperCase moduleName }}', response.data);
      });
  },
};
