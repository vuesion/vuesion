export const {{ camelCase moduleName }}DefaultState: any = {
  {{ camelCase moduleName }}: [],
};

export const {{ camelCase moduleName }}Mutations = {
  {{ upperCase moduleName }}: (state: any, {{ camelCase moduleName }}: any) => {
    state.{{ camelCase moduleName }} = {{ camelCase moduleName }};
  },
};
